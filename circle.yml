---
machine:
  environment:
    DB: postgres
  services:
    - postgresql
  ruby:
    version: 2.2.3

test:
  override:
    - bundle exec rake GEMNAME=spree_api_v2
    - bundle exec rake GEMNAME=solidus_api_v2
